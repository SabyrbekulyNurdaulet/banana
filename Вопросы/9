# 9.  Списки кортежи словари.

### 1. Списки (Lists)

Список представляет собой упорядоченную коллекцию элементов, которые могут быть различных типов данных. Элементы списка могут изменяться.

```python
pythonКопировать код
# Создание списка
my_list = [1, 2, 3, 4, 5]

# Доступ к элементам списка
print(my_list[0])  # Вывод: 1

# Изменение элементов списка
my_list[0] = 10
print(my_list)  # Вывод: [10, 2, 3, 4, 5]

# Добавление элемента в конец списка
my_list.append(6)
print(my_list)  # Вывод: [10, 2, 3, 4, 5, 6]

# Удаление элемента из списка
del my_list[0]
print(my_list)  # Вывод: [2, 3, 4, 5, 6]

# Проход по элементам списка
for item in my_list:
    print(item)

```

### 2. Кортежи (Tuples)

Кортеж представляет собой упорядоченную коллекцию элементов, которые могут быть различных типов данных. Основное отличие кортежей от списков заключается в том, что кортежи неизменяемы.

```python
pythonКопировать код
# Создание кортежа
my_tuple = (1, 2, 3, 4, 5)

# Доступ к элементам кортежа
print(my_tuple[0])  # Вывод: 1

# Попытка изменить элемент кортежа (приведет к ошибке)
# my_tuple[0] = 10  # TypeError: 'tuple' object does not support item assignment

# Проход по элементам кортежа
for item in my_tuple:
    print(item)

```

### 3. Словари (Dictionaries)

Словарь представляет собой неупорядоченную коллекцию данных в формате ключ-значение. Каждый элемент словаря состоит из пары ключа и соответствующего ему значения.

```python
pythonКопировать код
# Создание словаря
my_dict = {"name": "Alice", "age": 30, "city": "New York"}

# Доступ к элементам словаря по ключу
print(my_dict["name"])  # Вывод: Alice

# Изменение значения элемента словаря
my_dict["age"] = 35
print(my_dict)  # Вывод: {'name': 'Alice', 'age': 35, 'city': 'New York'}

# Добавление нового элемента в словарь
my_dict["gender"] = "female"
print(my_dict)  # Вывод: {'name': 'Alice', 'age': 35, 'city': 'New York', 'gender': 'female'}

# Удаление элемента из словаря
del my_dict["city"]
print(my_dict)  # Вывод: {'name': 'Alice', 'age': 35, 'gender': 'female'}

# Проход по элементам словаря
for key, value in my_dict.items():
    print(f"{key}: {value}")

```
