# **30. В чем разница между append и extend?**

Методы `append()` и `extend()` в Python используются для добавления элементов к списку, но есть разница в том, как они добавляют элементы:

1. **`append()`**:
    - Метод `append()` добавляет указанный элемент к концу списка как один элемент.
    - Если элементом, который вы пытаетесь добавить, является другой список или любой другой итерируемый объект, этот объект добавляется как один элемент.
    - После использования `append()` список будет содержать только один элемент - добавленный элемент или добавленный объект в целом.

Пример:

```python
my_list = [1, 2, 3]
my_list.append(4)
print(my_list)  # Вывод: [1, 2, 3, 4]

my_list.append([5, 6])
print(my_list)  # Вывод: [1, 2, 3, 4, [5, 6]]

```

1. **`extend()`**:
    - Метод `extend()` добавляет элементы указанного итерируемого объекта (например, списка) к концу текущего списка.
    - В отличие от `append()`, элементы добавляемого объекта добавляются по одному, а не в виде одного элемента.
    - После использования `extend()` список будет содержать все элементы из добавляемого объекта.

Пример:

```python
my_list = [1, 2, 3]
my_list.extend([4, 5])
print(my_list)  # Вывод: [1, 2, 3, 4, 5]

```

Таким образом, основная разница между `append()` и `extend()` заключается в том, что `append()` добавляет один элемент или один объект, а `extend()` добавляет все элементы из указанного итерируемого объекта.
