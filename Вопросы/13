# 13. Как проверить, что все символы строки относятся к алфавитно-цифровым?

Для проверки того, что все символы строки являются алфавитно-цифровыми, можно использовать метод строки `isalnum()`. Этот метод возвращает `True`, если все символы строки являются алфавитно-цифровыми (буквами или цифрами), и `False` в противном случае. Вот пример:

```python
pythonКопировать код
# Строка содержит только алфавитно-цифровые символы
str1 = "Hello123"
print(str1.isalnum())  # Вывод: True

# Строка содержит символы, не являющиеся алфавитно-цифровыми
str2 = "Hello123!"
print(str2.isalnum())  # Вывод: False

```

В этом примере `str1.isalnum()` вернет `True`, так как все символы строки "Hello123" являются буквами или цифрами. А `str2.isalnum()` вернет `False`, так как символ "!" не является алфавитно-цифровым.
