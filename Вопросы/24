# **24. Назовите изменяемые и неизменяемые объекты**

В Python объекты делятся на изменяемые (mutable) и неизменяемые (immutable) в зависимости от того, может ли объект быть изменен после создания. Вот некоторые примеры изменяемых и неизменяемых объектов:

**Неизменяемые (immutable) объекты:**

1. Числа (int, float)
2. Строки (str)
3. Кортежи (tuple)

Примеры:

```python
x = 5  # int (неизменяемый)
s = "hello"  # str (неизменяемый)
t = (1, 2, 3)  # tuple (неизменяемый)

```

Попытка изменить неизменяемый объект приведет к созданию нового объекта.

**Изменяемые (mutable) объекты:**

1. Списки (list)
2. Словари (dict)
3. Множества (set)

Примеры:

```python
lst = [1, 2, 3]  # list (изменяемый)
dct = {'a': 1, 'b': 2}  # dict (изменяемый)
st = {1, 2, 3}  # set (изменяемый)

```

Изменяемые объекты могут быть изменены после создания без создания нового объекта. Например, вы можете добавлять, удалять или изменять элементы списка, словаря или множества.
